{
  "frontend/src/pages/DashboardPage.tsx": "Performance dashboard displaying mock metrics (weekly volume, avg vest %, frequency, elevation, stability) with pull-to-refresh support via wrapper function.",
  "frontend/src/pages/ProfilePage.tsx": "Profile settings page for updating athlete data (bodyweight, height, experience, sport, goals, frequency) and account deletion with proper enum type usage.",
  "frontend/src/pages/UpgradePage.tsx": "Subscription upgrade page allowing users to choose between Monthly Pro and Annual Pro tiers with discount code support using proper SubscriptionTier enum.",
  "frontend/src/App.tsx": "Main app component with TanStack Router setup; RootComponent now includes a startup gate that shows AppLoadingScreen while auth/profile state is resolving, preventing black screens on initial load; router guards now throw errors on timeout instead of swallowing them; includes production-safe startup phase diagnostics.",
  "frontend/src/components/routing/AppLoadingScreen.tsx": "Full-screen dark-theme loading component with explicit English text explaining startup process (\"Starting EVRVEST\" / \"Loading your authentication and profile...\") to avoid blank/vague feel during auth/profile resolution.",
  "frontend/src/components/routing/RouteErrorFallback.tsx": "Dark-theme error fallback UI with enhanced recovery action that clears all startup session state (via clearAllStartupState utility) before reloading; detects startup/auth errors and provides context-specific messaging and recovery instructions in English.",
  "frontend/src/pages/LoginPage.tsx": "Login page with Internet Identity integration; removed useEffect-based navigation, now relies on router lifecycle guards for redirects, shows AppLoadingScreen during initialization or login using isLoggingIn helper boolean, and renders login UI only for unauthenticated users.",
  "frontend/src/pages/OnboardingPage.tsx": "Multi-step onboarding flow for creating athlete profiles; removed useEffect-based navigation, now relies on router lifecycle guards for auth/profile redirects, shows AppLoadingScreen during initialization or when unauthenticated (router will redirect).",
  "frontend/src/lib/routerAuthStore.ts": "External store bridging React auth/profile state into TanStack Router lifecycle hooks; waitUntilResolved now throws descriptive errors on timeout with phase context (identity-init, profile-load) and captures diagnostic metadata (lastResolvedPhase, lastError, timestamp) for troubleshooting without exposing sensitive data.",
  "frontend/src/pages/NotFoundPage.tsx": "Not Found page component displaying a 404 error with clear English text and a context-aware action button that routes to Dashboard when authenticated or Login when unauthenticated, matching the EVRVEST dark theme.",
  "frontend/src/utils/urlParams.ts": "URL parameter parsing and session storage utilities; added clearAllStartupState function that removes all sessionStorage items and clears URL parameters from hash to enable deterministic startup reset when RouteErrorFallback recovery is invoked."
}